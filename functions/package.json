{
  "name": "taleos-functions",
  "description": "Backend Taleos (Cloud Functions) — logique métier hors extension",
  "engines": {
    "node": ">=22"
  },
  "main": "index.js",
  "private": true,
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint ."
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^5.1.1"
  }
}
